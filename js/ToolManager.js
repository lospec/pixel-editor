const ToolManager = (() => {
    /** What it should do
     * 
     * - Listen to mouse events
     *      - Bind mouse events
     *      - Call tool functions
     * 
     *  
     */

    return {

    }
})